#include <stdio.h>
/*
 note always in the operation of integer and interger there always comes integer.
 
 note always in the operation of integer and float there always comes float values.
 
 note always in the operation of float and float there always comes float values.
 
 should be there and the retuning values always be an float values  


*/
int main () {
	
	int a = 5,c = 4;
	float b = 3.24;
    printf("The value of a + c is : %d\n",a+c);
    printf("The value of a + b is : %f\n",a+b);
    
	return 0;
}
