#include <stdio.h>

int main (){
	
	// Write a program in C language to swap to vaiables
	// Decalaring the variables
    int  a = 100;
    int b = 50;
    int c;
    
    // In this module we can change the function or SWAPING of the vairables
    c = a;
    a = b;
    b = a;
    
    // In this module we can print value of the variables on the output screen 
    printf("The value of a is %d\n",a);
    printf("The value of b is %d\n",b);
    
	return 0;
    
}
