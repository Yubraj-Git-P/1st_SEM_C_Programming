#include <stdio.h>

int main ()

{
    /*//char user[15];
		
    //printf(" Enter the value the string :");
    //scanf("%s",user);
    
   // printf(" Your enter user value for string is %s",user);
     
    char string[50];
	
	printf(" Enter your name : ");
	
	gets(string);// gets is a function which is already predefined in c languange  which helps 
	// to take multiple sting values from the user ..
	// gets is a function which is already predefined in a c language which helps 
	// to take muiltiple string values form the user	  
    // gets function helps us to take multiple string values from the user
    // gets fucniton helps us to take multiple string values formt he user
    // gets fucntion helps us to take multiple string values from the user
    // gets fucntion helps us to take multiple string values from th use
    // gets fucntion helps us to take multiple string values from teh user
    // gets funciton helps us to take multiple string values from the user
    // gets fucntion helps us to take multiple string values from the user
    // gets fcuntion helps us to take multiple string values from the user
    // getes funtion helops us to take multiple stirng values from the user
	puts(string);//puts is a predefined function in c language which is used to print an multiple
	// string values from the user
	// puts is predefined fucntionn in c language which is used to print an multiple string valeus
	// put()  is a fucntion which is used to print multiple string values from the user
	// puts is a fucntion which is used to print multiple string values from te user
	// puts is a function which is used to print multiple string values from the user
	// puts is a function which is used to print multiple sting  values from the uer
   // printf(" Your enter sting %s",string);*/
     
     
   // printf("***************************************************\n"); 
     
    /*char name[20];
    printf("Enter your name :");
    gets(name);// gets is a predefined funcition which is use to enter multiple string values from user
    puts(name);// puts is a predefined function which is used  to enter multiple string values from use
     
    char address[30];
	printf(" Enter your address :");
	gets(address);//gets is a predefined function which is used to enter multiple string value from use
	puts(address);// puts funciton helps us to print an multiple string values in the output board
	
	char fathername[40];
	printf(" Enter your fathe name :");
	gets(fathername);// gets is a function which is used to enter multiple string values from user
	puts(fathername);// puts funcion helps us to print an multiple string values in the output screen
	
	char mothername[50];
	printf(" Enter your mother name :");
	gets(mothername);// gets fucntion is used to print an multiple string values from user
	puts(mothername);// puts funciton helps us to print an multiple string values fromt the user*/
     
	printf("**********************************************************\n");
	printf("**********************************************************\n");
	printf("**********************************************************\n");
	
//	char name[] = "YUBRAJ";
	
//	name = "sahil";
	
//	printf("%s",name);
	
	// BUT
	
	char *hum = "YUBRAJ";
	// But by intializatiang the valu of char by using pinter we can acess the value of the
	// string array even after decalaring it also::::
	hum = "sahil";
	
	printf("%s",hum);    
	    
	return 0;
}
